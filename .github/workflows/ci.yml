name: CI
on:
  push:
  workflow_dispatch:
jobs:
  Minify_CSS:
    name: Minify CSS
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
      - name: Install NPM dependencies
        run: npm clean-install
      - name: Check that minified CSS is up to date
        run: |
          node_modules/.bin/lightningcss --minify oatcake.css --output-file oatcake.min.css
          if [[ -n "$(git status --porcelain oatcake.min.css)" ]]; then
            echo "oatcake.min.css is out of date"
            exit 1
          fi
